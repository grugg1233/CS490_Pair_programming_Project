[project]
name = "sakila-server"
version = "0.1.0"
description = "The backend for Sakila project."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.2",
    "flask-cors>=6.0.2",
    "mysql-connector-python>=9.5.0",
]
